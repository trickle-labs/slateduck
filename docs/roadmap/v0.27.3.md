# v0.27.3 — Testing Completeness, CI Production Gates & Documentation

> Hardened the quality infrastructure so every gap identified in Assessments
> 1–3 is structurally prevented, not just fixed point-in-time. Converts
> warning-level quality gates to hard failures, expands the test portfolio,
> and aligns all documentation with the current implementation.

## Key Changes

### Coverage as a Hard Gate (N-09)
- CI now fails with `exit 1` when workspace coverage drops below 80 %
- Per-crate minimums: `rocklake-core` ≥ 85 %, `rocklake-catalog` ≥ 85 %,
  `rocklake-sql` ≥ 80 %, `rocklake-pgwire` ≥ 75 %
- `rocklake-sqlite-vfs` removed from coverage crate list (already deleted)

### Doc-Tests for All Public APIs (N-10)
- `#![deny(missing_docs)]` added to `rocklake-core` and `rocklake-catalog`
- `# Examples` doc-tests added to all key public functions in
  keys.rs, values.rs, types.rs, store.rs, and reader.rs
- All `cargo test --doc --workspace` doc-tests pass

### Network-Level PG-Wire Integration Test (N-11)
- `crates/rocklake-pgwire/tests/pgwire_network_test.rs` added with 5 tests:
  full DDL/DML/query cycle, `SELECT version()`, TLS rejection,
  TLS-optional acceptance, and auth rejection
- `network-integration` CI job added

### Checkpoint Restore Snapshot-ID Safety (F-07)
- `crates/rocklake-catalog/tests/checkpoint_restore.rs` added:
  2 tests verifying post-restore snapshot IDs are strictly greater than
  all pre-restore IDs

### Concurrent Writer Fencing
- `crates/rocklake-catalog/tests/concurrent_writer_fencing.rs` added:
  3 tests covering stale-writer fencing, re-open after drop, and
  concurrent open with `tokio::join!`

### Metrics Documentation Alignment (Medium-10)
- `docs/operations/monitoring.md` rewritten to list only the 12 metrics
  actually emitted by `CatalogMetrics::render_prometheus()`
- Stale metric names removed; alert rules and Grafana panels updated

### Sanitizer & Miri Hardening
- `continue-on-error: true` removed from ASAN, UBSAN, and Miri CI jobs
- Miri coverage extended to `rocklake-core` (key/value encode/decode)

### Documentation Drift
- `docs/architecture/crate-structure.md`: `rocklake-sqlite-vfs` removal documented
- `docs/roadmap/v0.27.md`, `v0.27.1.md`, `v0.27.2.md`, `v0.27.3.md` created
- `docs/internals/open-findings-verification.md`: v0.27.3 section added

## Assessment Findings Addressed

| Finding | Status |
|---------|--------|
| N-09 — Coverage hard gate | ✅ Fixed |
| N-10 — Doc-tests for public APIs | ✅ Fixed |
| N-11 — Network-level PG-Wire test | ✅ Fixed |
| F-07 — Checkpoint restore snapshot-ID reuse | ✅ Fixed |
| Medium-10 — Metrics docs alignment | ✅ Fixed |
| Concurrent writer fencing | ✅ Fixed |
